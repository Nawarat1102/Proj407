import streamlit as st

# ตั้งค่าหน้าเว็บ
st.set_page_config(page_title="คู่มือการใช้งาน", page_icon="🔍")

# --- เนื้อหาของหน้าคู่มือ ---
st.title("คู่มือการใช้งานโปรแกรม 📖")
st.write("คำแนะนำและรายละเอียดการใช้งานโปรแกรมคำนวณด้วยระเบียบวิธีแก้ตำแหน่งผิด")
st.write("---")

# ใช้ st.header และ emoji เพื่อความน่าสนใจ
st.header("🎯 1. วัตถุประสงค์")
st.write(
    """
    โปรแกรมนี้ถูกพัฒนาเพื่อช่วยผู้ใช้คำนวณค่ารากของสมการด้วยวิธี **False Position Method** โดยมีเป้าหมายเพื่อ:
    - ใช้สำหรับหาค่ารากของสมการในช่วงที่กำหนด `[a, b]`
    - แสดงผลลัพธ์เป็นตารางแสดงขั้นตอนการคำนวณอย่างละเอียด
    - แสดงกราฟเพื่อให้เห็นภาพความคืบหน้าของการลู่เข้าสู่รากของสมการ
    """
)

st.header("🛠️ 2. วิธีใช้งาน (Step-by-step)")
st.info(
    """
    1.  **ไปที่หน้า 'การคำนวณ'**: เริ่มต้นจากเมนูหลัก
    2.  **ป้อนสมการ:** กรอกสมการที่ต้องการในช่อง `f(x)`
    3.  **กำหนดช่วง:** ระบุค่าขอบเขตซ้าย `a` และขอบเขตขวา `b` ที่คาดว่ามีรากอยู่
    4.  **ตั้งค่า Tolerance:** กำหนดค่าความคลาดเคลื่อนที่ยอมรับได้ (ยิ่งน้อยยิ่งแม่นยำ)
    5.  **กดปุ่ม 'เริ่มคำนวณ':** โปรแกรมจะเริ่มการประมวลผล
    6.  **ดูผลลัพธ์:** ตรวจสอบตารางและกราฟที่ปรากฏขึ้น
    """
)
st.write("ตัวอย่างการกรอกสมการ:")
st.code("x**3 - x - 2", language="python")


st.header("📊 3. คำอธิบายผลลัพธ์")
st.write(
    """
    - **a, b:** คือขอบเขตของช่วงที่ใช้ในการคำนวณในแต่ละรอบ
    - **c:** คือค่าประมาณของรากที่คำนวณได้ในรอบนั้นๆ
    - **f(c):** คือผลลัพธ์ของฟังก์ชันเมื่อแทนค่าด้วย `c` (เป้าหมายคือทำให้ค่านี้เข้าใกล้ 0 มากที่สุด)
    - **กราฟ:** แสดงจุด `c` ที่คำนวณได้ในแต่ละรอบ เพื่อให้เห็นว่าค่าประมาณเข้าใกล้รากจริงมากน้อยเพียงใด
    """
)

st.header("⚠️ 4. ข้อควรระวัง / ข้อจำกัด")
# ใช้ st.warning สำหรับหัวข้อนี้จะเหมาะสมมาก
st.warning(
    """
    - **เงื่อนไขสำคัญ:** ค่าของ `f(a)` และ `f(b)` **ต้องมีเครื่องหมายต่างกัน** (คูณกันแล้วได้ค่าน้อยกว่า 0) มิฉะนั้นโปรแกรมจะไม่สามารถหาคำตอบได้
    - **ความแม่นยำ:** ผลลัพธ์จะแม่นยำแค่ไหน ขึ้นอยู่กับค่า **Tolerance** ที่คุณตั้งไว้
    - **การลู่เข้าช้า:** สำหรับฟังก์ชันที่มีความโค้งสูงใกล้ราก อาจทำให้การลู่เข้าสู่คำตอบช้ามากได้ (One-sided convergence)
    """
)

st.header("✅ 5. ตัวอย่างการใช้งาน")
# ใช้ st.success เพื่อให้ดูเด่น
with st.success("ตัวอย่างที่ 1: การหารากของสมการ x³ - x - 2 = 0"):
    st.write("- **สมการ:** `x**3 - x - 2`")
    st.write("- **ช่วงที่กำหนด:** `a = 1`, `b = 2`")
    st.write("- **Tolerance:** `0.001`")
    st.write("เมื่อกดคำนวณ โปรแกรมจะแสดงตารางและกราฟแสดงการลู่เข้าสู่รากประมาณ `1.521`")

st.header("❓ 6. คำถามที่พบบ่อย (FAQ)")
# ใช้ st.expander ทำให้หน้าเว็บไม่ยาวเกินไป
with st.expander("คลิกเพื่อดู: ทำไมโปรแกรมถึงแจ้งว่า 'f(a) และ f(b) ต้องมีเครื่องหมายต่างกัน' ?"):
    st.write(
        """
        เพราะเป็นเงื่อนไขหลักของระเบียบวิธีนี้ครับ การที่ `f(a)` และ `f(b)` มีเครื่องหมายต่างกัน (ตัวหนึ่งอยู่เหนือแกน x อีกตัวอยู่ใต้แกน x) 
        เป็นการรับประกันว่ากราฟของฟังก์ชันจะต้องตัดผ่านแกน x (ซึ่งก็คือรากของสมการ) อย่างน้อยหนึ่งครั้งในช่วง `[a, b]`
        """
    )


with st.expander("คลิกเพื่อดู: ทำไมค่า c ถึงลู่เข้าช้ามาก?"):
    st.write(
        """
        อาจเกิดขึ้นได้หากกราฟของฟังก์ชันมีความโค้งสูงใกล้ๆ กับราก ทำให้ขอบเขต `a` หรือ `b` ด้านใดด้านหนึ่งแทบจะไม่ขยับเลย 
        ซึ่งเป็นหนึ่งในข้อจำกัดของระเบียบวิธีนี้ครับ
        """
    )
